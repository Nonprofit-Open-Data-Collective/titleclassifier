% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify-titles-parallel.R
\name{classify_titles}
\alias{classify_titles}
\title{Classify Part VII titles in parallel}
\usage{
classify_titles(df, batch_size = 2000, workers = NULL, preserve_input = FALSE)
}
\arguments{
\item{df}{A Part VII compensation data frame.}

\item{batch_size}{Number of filings per worker batch.}

\item{workers}{Number of parallel workers; defaults to half the cores.}

\item{preserve_input}{If \code{TRUE}, return the original columns intact with the
classification fields appended (joined by a synthetic row key). If \code{FALSE}
(default), return the pipeline's standardized columns.}
}
\description{
Runs the full title-cleaning and classification pipeline over a Part VII
compensation data frame, batching by filing (\code{OBJECTID}) across worker
processes. Returns one row per person-title.
}
\details{
By default the returned frame uses the pipeline's standardized (renamed,
cleaned, and pruned) columns. Set \code{preserve_input = TRUE} to instead return
the \strong{original input frame untouched} — every original column kept verbatim
in name and value, including columns the pipeline would normally rename or
drop — with the classification fields appended. Because a multi-title person
expands into multiple rows, each original row is repeated once per title.
}
