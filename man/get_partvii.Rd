% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch-tables.R
\name{get_partvii}
\alias{get_partvii}
\title{Fetch and read Part VII compensation tables in one call}
\usage{
get_partvii(
  years,
  dest = "efdata",
  table = .partvii_table_id(),
  root = .nccs_efile_root(),
  overwrite = FALSE,
  retry_max = 3L,
  timeout = 600,
  verbose = TRUE
)
}
\arguments{
\item{years}{Integer vector of tax years (e.g. \code{2023} or \code{2020:2023}).}

\item{dest}{Local cache directory. Created if it does not exist.}

\item{table}{Table id (file basename). Defaults to the Part VII
compensation table; exposed so the same machinery can fetch sibling tables.}

\item{root}{Base URL of the efile store. Defaults to the public NCCS v2 store.}

\item{overwrite}{Re-download even if the file already exists. Default \code{FALSE}.}

\item{retry_max}{Number of download attempts per file. Default \code{3}.}

\item{timeout}{Per-file download timeout in seconds. Default \code{600}.}

\item{verbose}{Print progress messages. Default \code{TRUE}.}
}
\value{
A data frame with the raw Part VII schema.
}
\description{
Convenience wrapper: \code{fetch_partvii()} followed by \code{read_partvii()}. Returns
a raw Part VII data frame ready to pipe into \code{standardize_df()}.
}
\examples{
\dontrun{
df <- get_partvii( 2023 ) |> standardize_df()
}

}
