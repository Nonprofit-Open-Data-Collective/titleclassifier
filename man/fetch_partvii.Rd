% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch-tables.R
\name{fetch_partvii}
\alias{fetch_partvii}
\title{Fetch Part VII compensation tables from the NCCS efile store}
\usage{
fetch_partvii(
  years,
  dest = "efdata",
  table = .partvii_table_id(),
  root = .nccs_efile_root(),
  overwrite = FALSE,
  retry_max = 3L,
  timeout = 600,
  verbose = TRUE
)
}
\arguments{
\item{years}{Integer vector of tax years (e.g. \code{2023} or \code{2020:2023}).}

\item{dest}{Local cache directory. Created if it does not exist.}

\item{table}{Table id (file basename). Defaults to the Part VII
compensation table; exposed so the same machinery can fetch sibling tables.}

\item{root}{Base URL of the efile store. Defaults to the public NCCS v2 store.}

\item{overwrite}{Re-download even if the file already exists. Default \code{FALSE}.}

\item{retry_max}{Number of download attempts per file. Default \code{3}.}

\item{timeout}{Per-file download timeout in seconds. Default \code{600}.}

\item{verbose}{Print progress messages. Default \code{TRUE}.}
}
\value{
A character vector of local file paths (one per requested year),
returned invisibly.
}
\description{
Downloads one or more years of the Form 990 Part VII compensation table
(\verb{F9-P07-T01-COMPENSATION-<year>.CSV}) from the public NCCS efile store to a
local cache directory. Files already present are reused unless
\code{overwrite = TRUE}. Returns the local file paths (invisibly) so the result
can be piped into \code{read_partvii()}.

Each yearly file is large (~1 GB), so downloads are cached on disk.
}
\examples{
\dontrun{
f  <- fetch_partvii( 2023 )
d  <- read_partvii( f )
df <- standardize_df( d )
}

}
