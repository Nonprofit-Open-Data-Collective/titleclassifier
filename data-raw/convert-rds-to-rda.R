all.titles <- readRDS( "all.titles.rds" )
save( all.titles, file="../data/all-titles.rda" )

former.words <- readRDS( "former.words.RDS" )
save( former.words, file="../data/former-words.rda" )

future.words <- readRDS( "future.words.RDS" )
save( future.words, file="../data/future-words.rda" )

interim.words <- readRDS( "interim.words.RDS" )
save( interim.words, file="../data/interim-words.rda" )

likely.subjects <- readRDS( "likely.subjects.RDS" )
save( likely.subjects, file="../data/likely-subjects.rda" )

likely.titles <- readRDS( "likely.titles.RDS" )
save( likely.titles, file="../data/likely-titles.rda" )

possible.regional.list <- readRDS( "possible.regional.list.RDS" )
save( possible.regional.list, file="../data/possible-regional-list.rda" )

possible.titles <- readRDS( "possible.titles.RDS" )
save( possible.titles, file="../data/possible-titles.rda" )

regional.words <- readRDS( "regional.words.RDS" )
save( regional.words, file="../data/regional-words.rda" )

status.mapping <- readRDS( "status.mapping.RDS" )
save( status.mapping, file="../data/status-mapping.rda" )